/**
 * Arrays provides u ordered collection
 * all types of data can be stored in a single array
 */

var arr = [12, "antman", true, 12.7];
console.log(arr);

/**
 * accessing the elements
 */

var a = arr[1];
console.log(a);
console.log(arr[3]);
/**
 * replacing elements
 */
arr[1] = "Superman";

console.log(arr);

/**
 * length property
 */

console.log("The lenght of the array is ", arr.length);
